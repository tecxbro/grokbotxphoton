import json,pathlib,hashlib,shutil,collections,re
root=pathlib.Path('/Users/darshan/Documents/ChatGPT/grokbotxphoton')
loc=json.loads(pathlib.Path('/tmp/wt00-integration-preflight-current.json').read_text());ev=pathlib.Path(loc['evidence']);snap=pathlib.Path(loc['snapshot']);meta=json.loads((ev/'snapshot.json').read_text());foundation=json.loads((snap/'docs/photon-features/foundation.json').read_text())
def sha(b):return hashlib.sha256(b).hexdigest()
out=root/'docs/photon-features/sources'/ev.name;out.mkdir()
index={'kind':'lane-source-record-aggregation','snapshotDigest':meta['snapshotDigest'],'includedLaneCommits':[],'note':'Records and authority metadata copied verbatim from the captured uncommitted snapshot. Aggregation does not validate or upgrade any lane claim. Duplicate URLs and inconsistent access results remain separate. No source record is promoted to SDK or live verification.','lanes':[]}
for n in range(10):
 lane=f'wt-{n:02}';p=snap/'docs/photon-features/evidence'/lane;d=json.loads((p/'sources.json').read_text());rows=d if isinstance(d,list) else d.get('records',d.get('sources',[]))
 dest=out/lane;dest.mkdir()
 files=[]
 for ext in ['json','md']:
  src=p/f'sources.{ext}';shutil.copyfile(src,dest/src.name);files.append({'originalPath':str(src.relative_to(snap)),'collectedPath':str((dest/src.name).relative_to(root)),'sha256':sha(src.read_bytes())})
 index['lanes'].append({'lane':lane,'reviewedCommit':None if n else meta['f0'],'recordCount':len(rows),'files':files,'originalDocument':d})
(out/'index.json').write_text(json.dumps(index,indent=2)+'\n')
md=['# WT-00 collected source index','','Captured snapshot: `'+meta['snapshotDigest']+'`. This is source aggregation, not integration acceptance. Original JSON documents and Markdown files are preserved byte-for-byte in lane subdirectories. Every access status, authority, version, hash, mismatch and limitation remains attributed to its lane. See index.json for the full records.','','| Lane | Records | Original JSON | Original Markdown | Reviewed lane commit |','| --- | ---: | --- | --- | --- |']
for lane in index['lanes']:
 l=lane['lane'];md.append(f'| {l} | {lane["recordCount"]} | [sources.json](./{l}/sources.json) | [sources.md](./{l}/sources.md) | '+(meta['f0'] if l=='wt-00' else 'Not supplied')+' |')
md+=['','Authority boundaries: exact pinned public types, inspected provider implementation, website documentation, supplementary skills, and authored `.mdx.vel` templates retain their separate source records. GitHub main is not an immutable package pin.','', 'Unresolved differences include HTTP 403 versus 200 observations across access methods/times; fetched versus read status; rolling website examples versus pinned Spectrum 12.8.0; generic clientGuid prose versus the public send contract; missing native poll identity/sequence in Spectrum; and absent public card-session restoration. No global best-status deduplication was applied.']
(out/'index.md').write_text('\n'.join(md)+'\n')
coverage=json.loads((snap/'docs/photon-features/reports/wt-09/operation-coverage.json').read_text());assert len(coverage['operations'])==44
shutil.copyfile(snap/'docs/photon-features/reports/wt-09/operation-coverage.json',ev/'wt09-operation-coverage-original.json')
lines=['# 44-operation preflight matrix','','WT-09 declarations retained from the captured snapshot. This table does not certify a production binding. Every operation has unknown account/conversation availability and no live evidence. All remain unintegrated. Original source snapshot and evidence are preserved in wt09-operation-coverage-original.json.','','| Operation | Owner | Lane declaration | Declared provider support | Tests / blockers |','| --- | --- | --- | --- | --- |']
for r in coverage['operations']:
 text='; '.join(r['tests'])+'; '+'; '.join(r['blockers'])
 lines.append('| '+' | '.join(str(v).replace('|','/').replace('\n',' ') for v in [r['operation'],r['owner'],r['laneDeclaredImplementation'],r['laneDeclaredProviderSupport'],text])+' |')
(ev/'operation-matrix.md').write_text('\n'.join(lines)+'\n')
ownership=json.loads((snap/'docs/photon-features/ownership.json').read_text());assignments=[]
for f in meta['manifest']:
 p=f['path'];owners=[]
 for lane,config in ownership['lanes'].items():
  prefixes=[ownership['packageRoot']+x for x in config['packagePaths']]+config['documentationPaths']
  if any(p.startswith(x) if x.endswith('/') else p==x for x in prefixes):owners.append(lane)
 if p in ownership['rootOwnedByWT00']:owners.append('wt-00')
 if owners:assignments.append({**f,'owners':owners})
(ev/'owned-path-snapshot.json').write_text(json.dumps({'note':'Path classification only; no reviewed commit attribution or ownership reassignment.','files':assignments},indent=2)+'\n')
report=['# WT-00 integration preflight: blocked before integration','','## Exact identity','',f'- Repository: {meta["repository"]}',f'- Worktree: `{meta["worktree"]}`; sole worktree; branch `{meta["branch"]}`.',f'- HEAD/F0: `{meta["head"]}`.',f'- Comparison and live remote main: `{meta["comparison"]}`; 1 ahead, 0 behind.',f'- F0 contract digest: `{foundation["contractDigest"]}` (71 files, 51 schemas; fresh check passed).',f'- Tested snapshot: `{meta["snapshotDigest"]}` at `{snap}`. This is a plain temporary file copy, not a branch/worktree.', '- Integration commit: none. Tested release commit: none. Included lane commits: none.', '- WT-01 through WT-09 reviewed commits: not supplied. Lane handoffs describe uncommitted work on the same main checkout.', '- Existing package.json/package-lock.json modifications and all existing lane files were preserved. Full dirty inventory and file hashes: snapshot.json.','','## Fresh verification','','Node 24.13.0, npm 10.9.2; package @grokbot/photon-features 0.1.0, existing CLI 0.2.2; Spectrum/core/iMessage 12.8.0, advanced-imessage 2.1.0, Zod 4.5.4, TypeScript 5.9.3, @types/node 24.10.1; SQLite from selected Node. Clean dependencies installed in the temporary snapshot with lifecycle scripts disabled. Global runtime unchanged.','','| Command | Exit / result |','| --- | --- |']
for c in meta['commands']:report.append('| `'+ ' '.join(c['argv'])+'` | '+str(c['exitCode'])+'; ['+c['name']+'.log](./'+c['name']+'.log) |')
report+=['','WT-09: 78 tests, 74 passed, 3 failed, 1 live test skipped. Offline provider fixtures and temporary SQLite/socket/installer tests do not prove provider delivery or deployment. `npm pack --dry-run` produced no final release artifact.','','## Blocking defects and ownership','','1. WT09-001: durable executor returns blocked/UNIMPLEMENTED for the production text module. WT-00 must settle shared per-provider-call execution services and request/claim access; WT-01/WT-03 own feature adoption. Do not certify the complete multi-call handler as single-call.','2. WT09-002: router and poll reducer both update inbox/continuation state, producing STALE_FENCE. WT-00 must assign one transactional owner; WT-02/WT-05 own adoption.','3. WT09-003: runtime SQLite is created 0644 under ordinary umask and installer rejects it. Establish DB/WAL/SHM private creation through the shared host/storage contract; preserve installer checks.','4. poll.get, poll.vote, poll.unvote, poll.addOption are explicitly unimplemented application integration. They are not provider blockers. WT-05 requests a scoped public advanced poll API extension with consistent snapshot/event semantics.','5. Production entrypoint, process lock, one actual store/outbox/SDK, aggregate registration, resource/media/stream bindings, authoritative grants, durable retrieval/claim/ack wiring and existing Grok notification binding remain unassembled. Real Grok wake/continuation is unverified.','6. No reviewed lane commits, approved integration workflow or exact clean assembled candidate. Packaging also requires bin metadata, aggregate integration gate and genuine workflow approval. No approval record was fabricated.','','## Required contract decisions','','Review each lane request under docs/photon-features/requests/wt-01 through wt-09. Publish a single exact shared amendment/digest only after integration inputs/workflow are resolved. Required decisions include execution child boundary; authoritative request/claim access; one inbox/handoff owner; private state creation; atomic media admission; card admission revision; recipient/admin grants; scoped poll extension/normalization; registry/codecs/exports; and host/process composition. No amendment or ownership reassignment was made here.','','## Source inventory and capability evidence','',f'Collected {sum(l["recordCount"] for l in index["lanes"])} records across WT-00–WT-09, preserving both original sources.json and sources.md. Shared index: [{out.name}]({out}/index.md).', 'All 44 operations, lane declarations, tests and blockers: [operation-matrix.md](./operation-matrix.md). These are attributed declarations and independent offline seam evidence, not 44 assembled successful calls. Source mismatches remain visible in the collected records.','','## Release gates','','- F0: existing committed foundation; fresh digest check passed.','- G1: fails production text seam; host not assembled.','- G2: fails missing poll application code and production bindings.','- G3: fails three independent WT-09 assertions; no exact assembled candidate.','- G4: not attempted; no releasable candidate or final artifact. Synthetic installer cases are not final artifact qualification.','- G5: not authorized or run.','','## Installation and rollback commands','','No concrete release checksum or installation command exists yet. The current tooling accepts these future command forms (placeholders are not executable release instructions):','','```sh','node packages/photon-features/scripts/package.mjs /absolute/assembled-candidate /absolute/approval.json /absolute/artifacts/release.gpf.gz','node /absolute/tools/install.mjs install /absolute/release.gpf.gz <sha256> /absolute/grok-photon','node /absolute/tools/install.mjs rollback /absolute/grok-photon <previous-release-sha256> confirm-inactive','```','','Keep package.mjs beside install.mjs. Installation selects inactive; activation remains separate. Preserve queued and unknown-outcome work. Only schema-1 compatibility currently has synthetic test evidence. No installation, activation, account provisioning, billing, hosting migration, permissions changes or live messages were performed.','','## Configuration and suspension limits','','Missing verified scope/line/phone mapping, authoritative context/intent grants, private credential configuration, selected transport, process owner/supervisor, original SDK resource restoration, card templates/backend authentication and existing Grok wake/task-acceptance binding. No production values were guessed or credentials accessed. During host suspension/offline periods, execution/wake cannot progress and in-flight calls may remain unknown. Public stream restart replay/cursor coverage is unverified; capture recovery does not establish an uptime or zero-loss guarantee. No keep-awake mechanism was added.','','## Concurrent changes observed','','These original files changed after capture; test evidence refers only to the retained snapshot:']
report += ['- `'+p+'`' for p in meta['sourceChangedDuringRun']]
report += ['','## Completion statements','','Code built: yes, captured dirty snapshot only.','Integrated: no.','Installed and activated: no.','Live verified: no.','','## Input required','','Provide each lane’s exact reviewed commit and approved integration workflow, or explicitly authorize reviewing and committing the current owned lane files here on main before integration. This is required by the user-provided instruction to use immutable reviewed lane commits and integrate only through the user-authorized workflow. Feature correction ownership remains with the existing lanes unless explicitly reassigned and recorded.']
(ev/'handoff.md').write_text('\n'.join(report)+'\n')
shutil.copyfile('/tmp/wt00-integration-preflight.py',ev/'capture-and-verify.py');shutil.copyfile('/tmp/wt00-preflight-report.py',ev/'generate-report.py')
print(json.dumps({'handoff':str(ev/'handoff.md'),'sources':str(out/'index.md'),'records':sum(l['recordCount'] for l in index['lanes']),'operations':44}))
