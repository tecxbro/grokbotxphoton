# WT-09 requirement coverage and remaining work

This matrix separates implemented tests from pending verification. A passing fixture test is never promoted to assembled or live evidence. Current counts and exact snapshot are in summary.json. Test paths below are relative to `packages/photon-features/tests/`.

| Required area | Implemented verification | Remaining work / blocker |
| --- | --- | --- |
| 1. All 44 operations | `lanes/wt-09/sdk-and-voice.test.ts` validates all fixtures/ownership; `coverage.mjs` enumerates all actual lane factory declarations and absent capability records | All 44 successful production invocations require the WT-00 assembled host/candidate. Native operations currently have schema/authorization evidence only. Four poll operations explicitly declare unimplemented integration. |
| 2. Real local invocation | Real CLI library, credential file, Unix IPC, authenticated protocol, SQLite submission, durable executor and production text module in `e2e/feature-runtime.test.ts` | Fails WT09-001. Provider boundary uses an offline Space fixture. WT-00 production registration/resource adapter is not supplied. |
| 3. Context/authorization | `security/context-scope.test.ts`: forged/expired/future/revoked grants, wrong principal, cross-project/account/line/chat, new recipients, admin intent, status/cancel disclosure, permission removal, model JSON injection | Re-run the same adversarial cases through the finalized host/executor for all operation families. Current tests do not certify an isolated OS sandbox. |
| 4. Idempotency/recovery | `e2e/local-roundtrip.test.ts`, `e2e/multipart-recovery.test.ts`, `security/claim-fencing.test.ts`: concurrent IPC duplicates, key conflicts, cancellation, revocation, stale generation/fences, lease expiry, actual subprocess exit after possible offline acceptance, persistent unknown outcome/no resend | Full feature-module crash/recovery is blocked by WT09-001; no provider-side exactly-once claim. The crash provider is explicitly offline. |
| 5. Multipart | Real executor/SQLite child dispatch: accepted earlier child retained on later compilation failure, retry skips it; unknown later child prevents blind replay | Executor tests use a controlled handler calling the real child boundary. WT-03's production journal is not integrated with that boundary; no assembled multipart claim. |
| 6. Inbound | `e2e/inbound-events.test.ts`: non-text attachment preservation, vote/unvote/edit/read identities on one message, arrival-independent replay, unresolved payload retention, outbound echo suppression, no receipt-loop continuation | Public SDK does not expose all raw event order/restart replay. Early poll/router integration fails WT09-002. No real provider interaction observed. |
| 7. Polls | `e2e/poll-restart.test.ts`: two polls, duplicate labels, multiple voters/options, unvote, added-option identity reconciliation, out-of-order observations, duplicate replay, DB reopen, atomic state/handoff rollback | Router + reducer fails WT09-002. Ordered independent-option semantics are an explicit host fixture, not asserted as available native ingress. Native lookup and a correlated real vote remain pending. |
| 8. Cards | `e2e/card-restart.test.ts`: actual module sends/updates, original target over three updates, refreshed metadata, stale revision rejection, fresh module with resolver, codec validation, missing rehydration, callback participant/scope/task/generation/nonce/expiry/replay/backend guards | Module receives real SQLite directly for its component tests; the executor's restricted store still needs integration. Customized extension parameters and real backend protocol/device rendering are not supplied. |
| 9. Typing | `e2e/typing-lifecycle.test.ts`: completion, worker failure, provider start/stop failures, abort, timeout, overlap/old stop, delayed expired start, delayed start/stop completion, nonblocking work, worker waiting, restart, connection loss/recovery | Actual SDK connection-loss signal and host binding remain unverified. The final host must wire bounded shutdown/connection handling; no device-visible typing evidence. |
| 10. Media | `security/media-access.test.ts`: traversal, symlink ancestors/leaves, hardlinks, opened-inode replacement, private/internal address fixtures, misleading URLs, redirect rebinding, mixed DNS answers, actual byte limits, interruptions, reader release, pending/unknown retention | DNS/request functions are controlled fixtures; real HTTPS pinned-socket/TLS behavior and races during the open syscall are not independently exercised. Staging download timeout/cleanup and per-operation provider path require final integration tests. No real internal target was contacted. |
| 11. Webhooks | `security/webhook-auth.test.ts`: missing/bad/stale/future HMAC, exact-byte tampering, alternate envelope, acceptance barrier before ack, controlled DB failure gives 503, real temporary capture files and SQLite | The test calls the real Request/Response handler offline; an external HTTP server/proxy/provider delivery is not tested. WT-00 must register the durable handler rather than the SDK fire-and-forget callback. |
| 12. Packaging/regression | Original CLI 31 tests; F0 60 tests; build/schema drift; generated skill/all examples; package excludes test files; voice naming/code/URL preservation and structured bypass; actual installer synthetic release clean/repeat/rollback; private fixture DB hash and pending/unknown records preserved | Runtime-created DB permissions fail WT09-003. Candidate packaging/executable exports and production activation need WT-00 assembly. Synthetic test metadata is not a real release attestation. |
| 13. Single ownership | `e2e/single-ownership.test.ts`: concurrent owner starts create one SDK; competing subscriptions rejected; startup failure cleanup; disabled host; repeated stop | Proves behavior of those objects. System-wide process lock, a second host process, and complete production wiring need the final host/activation path. |
| 14. Live tests | `live/authorized-smoke.test.ts` disabled by default, exact text action/approval digest and full scope required, expiry/candidate gates, real local client submit/status path, acceptance-only assertion | No exact account/line/conversation/action authorization, credentials, active host or assembled SHA supplied. Poll-create/real-vote and card/device live workflows remain pending; no pass is recorded for them. |

Remaining test work is intentionally visible. Do not declare WT-09 final acceptance or the entire product complete until the owner fixes, exact assembled candidate, uncovered integration paths and authorized live/device steps have been addressed. WT-09 cannot implement missing production seams or change shared contracts to bypass these requirements.
