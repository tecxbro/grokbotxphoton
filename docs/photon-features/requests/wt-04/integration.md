# WT-04 integration requests

F0: `57e40736be8a59047b776654c766fbe8bfd10c9e`, digest `e0f7779bc9ca3d45696e9b4e24e8579d922c8d2d7d4323c821202d8f599acbf8`.

WT-04 implements `createMediaModule`, `SafeMediaStager` (the exact F0 `MediaStager.resolve` port), `nativeMediaSource`, `mediaProvider`, `assertActionMediaAvailable`, and contact import/export helpers in `src/features/media/index.ts`. These are host APIs, not model operations.

1. **WT-00: exports and composition.** Add the media barrel to the package export surface, compose its four handlers and three compilers, and register both recovery codecs. Inject the existing shared store, context/resource resolver, clock and existing Spectrum instance. No client is created here. `ScopedMediaBinding` requires the complete logical scope, the native conversation ID, an explicit phone, and the existing provider's public `getAttachment` method. Use `mediaProvider(existingApp)` to obtain the public adapter.
2. **WT-01: atomic staged-reference admission.** Call `assertActionMediaAvailable(tx, parsedAction, trustedContext)` inside the same transaction that accepts every new or retried action, including nested avatar/background/card media. A committed staged-resource tombstone prevents future admission. Do not enable cleanup until this hook is connected. The current source resolver independently checks tombstones at read time, but that cannot guarantee a future queued action is accepted with retained resources without the atomic admission hook.
3. **WT-01/WT-02: authoritative IDs.** Populate the shared `references` rows for logical space/message/attachment IDs, with native `providerId`, owner, task, generation, scope, and parent identity. Never equate WT-02's opaque IDs with native iMessage GUIDs. `nativeMediaSource(resources, bindings, sharedStore)` requires those records before calling `getAttachment(nativeGuid, phone)`. It verifies the resolved native parent actually contains the attachment and belongs to the bound conversation/phone. Missing mappings reject safely.
4. **WT-02: inbound metadata.** Wire `NormalizedMediaLookup` to the typed snapshot/capture metadata when available, preserving the logical reference plus native attachment, message and conversation IDs, MIME, original name, claimed size and duration. The current `snapshotContent` voice branch omits `Voice.id`; preserve it when supplied by Spectrum instead of deriving an ambiguous generic voice ID. WT-04 saves validated metadata and read pins in the existing `checkpoints` table under `wt04.media-metadata@1`. Rich contact fields outside F0's name/phones/emails subset must remain quarantined; `normalizeContact`/`importVCard` provide the supported projection. No second message reducer is registered by WT-04.
5. **WT-00: execution identity.** Add authoritative request ID to `ExecutionServices` if desired. F0 lacks one; WT-04 currently resolves exactly one matching authoritative outbox action/context/idempotency key and checks its claim. At 1000 scoped rows it fails closed rather than relying on an incomplete scan.
6. **WT-00/WT-01: retention scalability and crash reconciliation.** Current cleanup conservatively retains all scoped staged files whenever pending/blocked/accepted/unknown operations, unresolved input, pending children/handoffs, foreign checkpoints, or a saturated scan exist. Read pins persist across process death and are never expired speculatively. Future exhaustive reference queries and proof that a crashed consumer can no longer access its resource can permit finer cleanup. Existing F0 has no delete primitive, so released records/checkpoints remain as tombstones. Do not remove these records while references can still arrive.

No contracts, migration, registry, package exports, dependency files, host wiring or WT-02 implementation were edited by WT-04. These requests are integration blockers, not claims of installed or live behavior.
