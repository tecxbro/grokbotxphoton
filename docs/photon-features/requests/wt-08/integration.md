# WT-08 shared integration requests

Basis: F0 `57e40736be8a59047b776654c766fbe8bfd10c9e`, digest `e0f7779bc9ca3d45696e9b4e24e8579d922c8d2d7d4323c821202d8f599acbf8`. No shared file was edited by WT-08.

1. Register package bin `grok-photon: dist/src/cli/main.js`. Extend the package files list to include SKILL.md, INSTALL.md, README.md, examples/wt-08, scripts/install.mjs, scripts/smoke-test.mjs, scripts/package.mjs and scripts/generate-skill.mjs as appropriate for the assembled distribution. The custom archive already selects required installation payloads explicitly; npm pack does not yet include the manual/examples.
2. Register offline aggregate `photon:test:integration` covering all implemented lanes and WT-09. Include WT-08's compiled CLI tests, source `distribution.test.mjs`, and generated-skill drift check. Retain existing CLI tests. Packaging refuses to proceed without this script and the bin registration.
3. Supply approved clean assembled candidate and trusted workflow approval per INSTALL.md. The packager reruns tests on exact HEAD, checks F0 digest, creates exact dependencies via npm ci --ignore-scripts, records platform/architecture and produces checksums/provenance. No release was generated here. Verify clean dependency archive contents and optional native codec behavior on each release target; no lifecycle scripts are enabled automatically.
4. Add shared runtime validation for LocalResponse/HandoffRecord if desired. F0 currently exposes work responses only as TypeScript interfaces. WT-08 validates that seam locally with Zod using shared scope/ID/event/result/capability schemas. The socket authenticator also returns failures without requestId, while LocalResponse currently requires it; WT-08 accepts this observed frozen wire behavior. Operation argument schemas remain exclusively shared. Replace the local response-only shape once WT-00 supplies a shared export.
5. Implement the deployment host entrypoint and process-wide owner protocol: startup must coordinate with `.install-lock`, hold `runtime/host.lock`, honor disabled activation, and release markers only through verified shutdown. Installer checks alone cannot prevent an uncooperative host from racing. Protect SQLite/WAL/SHM and local credential files under the 0700 runtime directory. Bind trusted context, readiness and submission ownership, with no SDK credentials in the CLI. Keep the existing wake/task-acceptance adapter and do not activate a replacement orchestrator.
6. Publish richer state/recovery-codec compatibility metadata before schema versions beyond 1. Current rollback permits only tested schema 1 and preserves state. Unknown/queued work is never dropped. Later migration/codec changes require explicit downgrade tests and tooling changes.
7. The frozen protocol has no status-by-idempotency-key lookup or acknowledged-handoff lookup. Preserve submitted request IDs and durable Grok task acceptance. A lost submit response before request ID persistence must be reconciled through the existing orchestrator/runtime; WT-08 never automatically resubmits. Add an authorized lookup seam through WT-00 if the assembled recovery workflow needs it.
8. Current root lock has only developer packages nested under packages/photon-features/node_modules. Distribution vendors the freshly installed root dependencies. If runtime dependencies become nested, extend the collector before release; do not silently claim dependency completeness. No new dependency is requested by WT-08.
