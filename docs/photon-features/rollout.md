# F0 rollout boundary

## Built checkpoint
Run the focused foundation, pinned SDK, checker failure tests, schema drift, full workspace typecheck/build, inherited foundation and original CLI regressions, ownership/docs and manual diff review. Commit only exact owned paths and source snapshots after all pass. Create photon-v3-f0 at that tested commit only if absent; never move it. Report the commit SHA after creation, outside that commit.

## Integration and activation
Only WT-00 exists for this assignment. The map defines WT-01 through WT-09 and integration paths but does not create them. Integration must occur in /Users/darshan/Documents/ChatGPT/grokbotxphoton-worktrees/wt-integration. Adapt inherited table-based feature services/private child hooks to the public f0-services-2 seam there, with owned-lane changes. All handlers must register honestly and assembled/security/recovery/package tests must pass before product readiness.

Installation, service activation, live credential setup, platform approval, provider delivery and physical-device testing remain outside F0. No deployment, hosting migration, line provisioning, billing or live messages occur here. Local dependency/toolchain installation is test setup, not runtime installation.
