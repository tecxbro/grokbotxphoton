# Integration reference index

Normative bodies are the SHA-256-locked snapshots under `docs/photon/reference`.
Lane-specific immutable snapshots remain under each included lane's
`docs/worktrees/wt-NN/references` directory. This directory records only
integration-specific source or verification artifacts added after assembly.
