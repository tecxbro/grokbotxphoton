# Change requests

## Shared contract decision
Adopt explicit executeChild and restricted UnitOfWork through new named public files. Preserve legacy ports for inherited code; integration lane must adapt features rather than access private runtime tables. No sibling lane edits authorized here.
