# WT-03 rules
Use the assigned registered WT-03 and f0-services-2 public execution services. Preserve four pre-existing relocation edits. Owned changes are limited to the requested text files, four test suites and this documentation. No live sends, shared edits, credentials or new runtime journal. Legacy compatibility is not public-host registration.
