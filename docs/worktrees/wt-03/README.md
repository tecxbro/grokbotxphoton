# WT-03 text and message actions
Implement all 13 assigned operations through createFeatureModule and injected provider bindings. Preserve compatible legacy entry points while the F0 host uses only the public factory. See ARCHITECTURE.md, ACCEPTANCE.md and HANDOFF.md.
