# Sources
Official Photon Markdown is normative; source-lock.json records exact retrieval status, identity and SHA256. Snapshots stay in this lane's references directory. Skills are separate workflow guidance: local Spectrum 3.1.0, iMessage 9.1.0, app-cards, provider/iMessage/apps and capability semantics. Installed public SDK declarations constrain implementation; record package versions and hashes after installation. Browser access to the official llms.txt index succeeded; focused .md browser reads failed, so direct retrieval records decide snapshot availability. No unavailable source is represented as read.

Direct anonymous retrieval subsequently succeeded for all seven official sources (HTTP 200 with validated Markdown content types, expected document identity and SHA256). Five upstream skill snapshots also returned HTTP 200 and are classified separately. source-lock.json records manifests/declarations hashes for spectrum-ts, @spectrum-ts/core and @spectrum-ts/imessage, all 12.8.0, plus hashes of locally read skill files. No package/document API disagreement was found for app/customizedMiniApp/edit/miniAppCardSession. The pinned public declaration describes session fields chatGuid, messageGuid, sessionId and targetMessageGuid; inspected provider behavior refreshes the original target's public metadata. No private SDK field is accessed in the new path.
