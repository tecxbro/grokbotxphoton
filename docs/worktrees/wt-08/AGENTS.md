# WT-08 instructions

Work only on the WT-08 CLI, generated operating skill, inactive distribution tools, assigned examples/tests, and this lane's documentation. The frozen `f0-services-2` contracts, shared registry/schemas/host, package metadata, root CLI, other lanes, archive/product snapshots, and relocation-only edits are read-only.

Use the authenticated local Unix-socket protocol; never create a Spectrum connection or public command-execution endpoint. Keep machine JSON on stdout, diagnostics on stderr, credentials out of both, and execution/provider/delivery/read evidence distinct. Installation and ordinary smoke checks remain inactive and offline. Build a final archive only from a clean approved assembled candidate.

Record shared verification limitations in `CHANGE-REQUESTS.md`. Stage only owned task hunks and preserve the four pre-existing relocation edits.
