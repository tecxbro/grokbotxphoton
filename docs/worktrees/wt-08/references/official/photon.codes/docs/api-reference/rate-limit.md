> ## Documentation Index
> Fetch the complete documentation index at: https://docs.photon.codes/docs/llms.txt
> Use this file to discover all available pages before exploring further.

# Rate limit

> Default request limits for the Spectrum API.

## Rate limit

The default maximum rate limit is **5 requests per second per project**. This limit applies across all requests authenticated with your project credentials. This number can be increased for trusted senders by request. After that, you'll hit the rate limit and receive a `429` response error code.
